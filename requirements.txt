# Core service
fastapi>=0.115,<1
uvicorn[standard]>=0.30

# Data & compute
numpy>=1.26,<2
pandas>=2.2
pyarrow>=15
pydantic>=2.6
python-dotenv>=1.0

# OpenAI SDK & HTTP
openai>=1.40,<2
httpx>=0.27

# Optional (comment in if you want lexical retrieval and more utilities)
scikit-learn>=1.5       # pulls scipy for TF-IDF
joblib>=1.4
# orjson>=3.9           # faster JSON on most platforms
# tqdm>=4.66            # progress bars
# tenacity>=8.2         # robust retries

# IMPORTANT: Install FAISS on Windows via conda, not pip:
#   conda install -c conda-forge faiss-cpu
